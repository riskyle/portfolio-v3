import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Christian Kyle",
  description: "Christian Kyle is an aspiring Software Engineer determined by growth, curiosity, and the desire to build meaningful, functional software. I’ve worked across the stack using tools like Laravel, React, Firebase, Nuxt, Docker, and Windows Subsystem for Linux (WSL) constantly pushing myself to learn and adapt",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
